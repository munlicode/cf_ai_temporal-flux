{
  "name": "cf-ai-temporal-flux",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.1.1",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^1.10.0"
  }
}